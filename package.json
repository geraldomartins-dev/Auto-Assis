{
  "name": "auto-assis",
  "version": "1.4.0",
  "private": true,
  "description": "Sistema web para gestão de oficina automotiva.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "verify": "node --check server.js && npm run check:html && npm test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "check:html": "node scripts/check-inline-scripts.js",
    "backup": "node scripts/backup-database.js",
    "migrate": "node scripts/migrate.js",
    "create-admin": "node scripts/create-admin.js"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mysql2": "^3.23.2",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
